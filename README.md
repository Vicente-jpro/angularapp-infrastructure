# Welcome to your CDK TypeScript project

This is a blank project for CDK development with TypeScript.

The `cdk.json` file tells the CDK Toolkit how to execute your app.

## Useful commands

* `npm run build`   compile typescript to js
* `npm run watch`   watch for changes and compile
* `npm run test`    perform the jest unit tests
* `npx cdk deploy`  deploy this stack to your default AWS account/region
* `npx cdk diff`    compare deployed stack with current state
* `npx cdk synth`   emits the synthesized CloudFormation template



### Authentication with amplify on front end
It generate web pages for user flow. It also create cognito resources

```sh
https://constructs.dev/packages/@aws-cdk/aws-amplify-alpha/v/2.236.0-alpha.0?lang=typescript
```

```sh
https://docs.amplify.aws/angular/build-a-backend/auth/
```

```sh
https://www.npmjs.com/package/aws-amplify
```






